apiVersion : v1
kind : Pod
metadata :
    name : kubetest
spec :
    containers :
        -
            image : luksa/kubia
            name : kubiatest
            ports :
                -
                    containerPort : 8080
                    protocol : TCP